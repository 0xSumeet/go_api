package config

const (
	// DefaultLimit and MaximumLimit defies the default and maximum amount of json data
	// needs to be fetched
	DefaultLimit int    = 10
	MaximumLimit int    = 20
	JWTSecret    string = "my_secret_key"
)
